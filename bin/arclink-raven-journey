#!/usr/bin/env bash
set -euo pipefail
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${repo_dir}/python${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 "${repo_dir}/python/arclink_raven_journey_harness.py" "$@"
