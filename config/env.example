# ArcLink SaaS Environment Variables
# Copy to .env and fill in values. Never commit secrets to version control.
#
# ARCLINK_* values take precedence over legacy ARCLINK_* aliases when non-empty.
# Blank ARCLINK_* values fall back to legacy settings.

# Product Identity
ARCLINK_PRODUCT_NAME=ArcLink
ARCLINK_BASE_DOMAIN=arclink.online
ARCLINK_PRIMARY_PROVIDER=chutes
ARCLINK_CONTROL_DEPLOYMENT_STYLE=single-machine  # single-machine, hetzner, or akamai-linode

# Chutes Model Provider
ARCLINK_CHUTES_BASE_URL=https://llm.chutes.ai/v1
ARCLINK_CHUTES_DEFAULT_MODEL=moonshotai/Kimi-K2.6-TEE
ARCLINK_MODEL_REASONING_DEFAULT=medium
# CHUTES_API_KEY=               # Required for live model catalog and key management

# Hosted API
# ARCLINK_CORS_ORIGIN=https://arclink.online   # Allowed CORS origin
# ARCLINK_COOKIE_DOMAIN=arclink.online          # Cookie domain attribute
ARCLINK_COOKIE_SECURE=1                          # Set 0 for HTTP-only dev
# ARCLINK_COOKIE_SAMESITE=Strict                 # Use Lax only for explicit legacy redirect compatibility
# ARCLINK_SESSION_HASH_PEPPER=                   # Required for production session/CSRF HMAC hashes
# ARCLINK_SESSION_HASH_PEPPER_REQUIRED=1          # Fail closed when the pepper is missing
ARCLINK_LOG_LEVEL=INFO
ARCLINK_DEFAULT_PRICE_ID=price_arclink_founders
ARCLINK_FOUNDERS_PRICE_ID=price_arclink_founders
ARCLINK_SOVEREIGN_PRICE_ID=price_arclink_sovereign
ARCLINK_SCALE_PRICE_ID=price_arclink_scale
ARCLINK_FIRST_AGENT_PRICE_ID=price_arclink_founders
ARCLINK_SOVEREIGN_AGENT_EXPANSION_PRICE_ID=price_arclink_sovereign_agent_expansion
ARCLINK_SCALE_AGENT_EXPANSION_PRICE_ID=price_arclink_scale_agent_expansion
ARCLINK_ADDITIONAL_AGENT_PRICE_ID=price_arclink_sovereign_agent_expansion
ARCLINK_FOUNDERS_MONTHLY_CENTS=14900
ARCLINK_SOVEREIGN_MONTHLY_CENTS=19900
ARCLINK_SCALE_MONTHLY_CENTS=27500
ARCLINK_FIRST_AGENT_MONTHLY_CENTS=14900
ARCLINK_SOVEREIGN_AGENT_EXPANSION_MONTHLY_CENTS=9900
ARCLINK_SCALE_AGENT_EXPANSION_MONTHLY_CENTS=7900
ARCLINK_ADDITIONAL_AGENT_MONTHLY_CENTS=9900

# Stripe
# STRIPE_SECRET_KEY=             # Required for live billing, portal links
# STRIPE_WEBHOOK_SECRET=         # Required for webhook signature verification

# Cloudflare DNS / Ingress
# CLOUDFLARE_API_TOKEN=          # Scoped token: Zone:DNS:Edit
# CLOUDFLARE_ZONE_ID=            # Target zone for arclink.online

# Docker Executor
# ARCLINK_EXECUTOR_LIVE=1        # Enable live Docker/provider execution (off by default)
# ARCLINK_CAPTAIN_MIGRATION_ENABLED=0  # Captain-initiated Pod migration remains disabled by default
# ARCLINK_MIGRATION_GC_DAYS=7          # Retain successful migration capture artifacts before GC

# Telegram Bot
# TELEGRAM_BOT_TOKEN=            # Public onboarding bot; fake mode when absent
# TELEGRAM_WEBHOOK_SECRET=       # Required for Telegram webhook secret-token verification

# Discord Bot
# DISCORD_BOT_TOKEN=             # Public onboarding bot; fake mode when absent
# DISCORD_APP_ID=                # Discord application ID for interaction verification

# Database
# ARCLINK_DB_PATH=               # SQLite path (default: arclink-priv/state/arclink-control.sqlite3)

# Legacy ArcLink (kept for backward compatibility)
# See config/arclink.env.example for the full legacy variable list.
# ARCLINK_NAME=arclink
# ARCLINK_ORG_NAME=
# ARCLINK_ORG_PROVIDER_SECRET=
