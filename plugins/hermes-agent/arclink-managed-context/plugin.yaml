name: arclink-managed-context
version: 1.0.0
description: Inject refreshed local ArcLink managed context into Hermes turns without mutating built-in MEMORY.md snapshots.
provides_hooks:
  - pre_llm_call
  - pre_tool_call
provides_commands:
  - start
