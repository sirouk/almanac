<<<CODEX-VERDICT-START CANON-14>>>
## CANON-14 — Codex (GPT-5.5 xhigh) ratification
SIGN-OFF: OBJECT(4)
ONE-LINE VERDICT: Core CANON-14 is correct, but the final canon should down-rank two Claude risks and add two queue/filesystem race findings.

### Adjudication (each: CONFIRM/REFUTE/REFINE + path:line)
- CONFIRM (MEDIUM): `recover_stale_actions` always returns stale `running` intents to `queued`, never terminal `failed`, and has no retry cap. Code says "queued" at `python/arclink_action_worker.py:2262` despite doc text at `python/arclink_action_worker.py:2247`; it commits and returns `new_status: queued` at `python/arclink_action_worker.py:2281-2283`.
- CONFIRM (MEDIUM): dismissed pin upgrades remain queueable. Raven reads `active_only=True` at `python/arclink_operator_raven.py:1281-1290`; "active" only means `applied_at IS NULL` at `python/arclink_control.py:9596-9608`; dismiss sets `silenced = 1` at `python/arclink_control.py:9683-9687`, which the active filter never reads.
- REFINE (§A10): typed non-Telegram Raven callers discovered here do enforce the approval code before dispatch: Curator Telegram at `python/arclink_curator_onboarding.py:345-359`, Discord at `python/arclink_curator_discord_onboarding.py:310-326`; Telegram does the same at `python/arclink_telegram.py:1305-1322`. Raven itself still only requires `actor_id` + `confirmed` at `python/arclink_operator_raven.py:400-420`, so transport-level framing is correct but not MEDIUM as stated.
- CONFIRM/REFINE (§A10): callback dispatch is the real code-skip residual. Telegram callback data `arclink:/...` dispatches directly at `python/arclink_curator_onboarding.py:822-838`; Discord component `custom_id` does the same at `python/arclink_curator_discord_onboarding.py:955-967`. Generated Raven buttons use `/upgrade_apply <nonce>` at `python/arclink_operator_raven.py:1404-1405`, so residual severity is LOW unless an attacker can inject arbitrary callback data.
- REFUTE mechanism / REFINE severity (§A10): `academy_apply` is not gated by one env var only. Worker passes executor adapter + `ARCLINK_ACADEMY_APPLY_LIVE` at `python/arclink_action_worker.py:1083-1096`; staging enables writes only when live adapter, live authorization, review readiness, and trainer readiness all pass at `python/arclink_academy_programs.py:2863-2864,2938-2940`. The filesystem writes are real at `python/arclink_action_worker.py:2089-2206`, but the original mechanism is wrong.
- CONFIRM (§B31 LOW): button nonce consume is read-check-write, not atomic. It SELECTs at `python/arclink_operator_raven.py:1381`, checks `used_at` at `python/arclink_operator_raven.py:1388`, then calls `upsert_setting` at `python/arclink_operator_raven.py:1394`; `upsert_setting` commits independently at `python/arclink_control.py:2971-2980`.
- CONFIRM/RESOLVE (§B31): the `public-agent-turn`/`operator_turn` seam is now both-end verified. Producer queues `target_kind="public-agent-turn"` with `operator_turn=True` at `python/arclink_operator_agent.py:271-278`; delivery routes that metadata to `_run_operator_agent_gateway_turn` at `python/arclink_notification_delivery.py:1576-1583`, whose control-stack gateway path is `python/arclink_notification_delivery.py:820-855`.
- REFINE: one-agent invariant is detected at the public boundary, not fully prevented at the first write. The pinned-setting guard is conditional at `python/arclink_operator_agent.py:111-117`; the count assertion runs after ensure returns deployment data at `python/arclink_operator_agent.py:373-379` and raises on `>1` at `python/arclink_operator_agent.py:198-215`.

### New findings both Claude passes missed (severity + path:line)
- MEDIUM: `operator_actions` is not an atomic queue. Enqueue dedupe is SELECT-then-INSERT with no transaction/unique key at `python/arclink_control.py:8273-8306`; schema has only a normal status/kind index at `python/arclink_control.py:760-772,1860-1862`. Drain is also SELECT pending then unconditional `UPDATE status='running'` at `python/arclink_control.py:8246-8257,8581-8589`, reached by provisioner at `python/arclink_enrollment_provisioner.py:2330-2342`. Concurrent request/drain can duplicate or double-run host/pin upgrades.
- MEDIUM: `academy_apply` path containment misses symlink ancestors under vault/Hermes paths. Relative path validation rejects absolute/`..` only at `python/arclink_action_worker.py:1414-1424`; materialization joins `vault / relative` and writes at `python/arclink_action_worker.py:2102-2105,2120-2135`; `_write_private_text_atomic` writes in `path.parent` and `os.replace`s without final resolved-parent containment at `python/arclink_action_worker.py:1384-1405`.
- LOW: action-worker executor selection can fail before an attempt row or failure event exists. `_select_action_executor` runs before `_record_attempt` at `python/arclink_action_worker.py:694-705`, while the catch/fail path starts only around dispatch at `python/arclink_action_worker.py:736-747,799-820`; such failures leave a claimed `running` row until stale recovery.

### Claude citations re-confirmed or corrected
- CONFIRMED: seven mutating Raven commands at `python/arclink_operator_raven.py:225`; mutating detection excludes dry-run at `python/arclink_operator_raven.py:301-306`; real queue paths are pod repair `python/arclink_operator_raven.py:1126-1135`, host upgrade `python/arclink_operator_raven.py:1558-1564`, rollout `python/arclink_operator_raven.py:1856-1865`.
- CONFIRMED: action worker atomic claim uses `BEGIN IMMEDIATE` plus rowcount guard at `python/arclink_action_worker.py:453-496`; dispatch status maps failed-closed/not-implemented to failed at `python/arclink_action_worker.py:750-772`.
- CONFIRMED: rollout is record-only: plan returns no mutation at `python/arclink_rollout.py:422-428`; worker forces `record_only=True` at `python/arclink_action_worker.py:1230-1238`; rollout executor contract accepts only fake/local + record_only at `python/arclink_rollout.py:837-864`.
- CONFIRMED: provisioner rechecks `request_source=="operator-raven"` before upgrade and pin-upgrade execution at `python/arclink_enrollment_provisioner.py:2292-2297,2334-2337,2451-2453`.
- CORRECTED: `PIN_UPGRADE_COMPONENTS` is imported policy, not local literal; source list is `python/arclink_upgrade_policy.py:9-17` and Raven imports it at `python/arclink_operator_raven.py:35`.

### Residual disagreement with the Claude half (for final reconciliation)
- Keep CANON-14 overall, but replace the original approval-code MEDIUM with LOW callback-path residual plus transport-enforced typed paths.
- Keep dismissed pin upgrades and stale-action infinite requeue as MEDIUM.
- Replace the original `academy_apply` MEDIUM rationale with: LOW for intended gated writes, MEDIUM for the newly found symlink containment gap.
- Add `operator_actions` atomicity as MEDIUM; it affects the host-upgrade/pin-upgrade lane more directly than the nonce double-consume race.
<<<CODEX-VERDICT-END CANON-14>>>
