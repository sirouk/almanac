<<<CODEX-VERDICT-START CANON-19>>>
## CANON-19 — Codex (GPT-5.5 xhigh) ratification
SIGN-OFF: OBJECT(5)
ONE-LINE VERDICT: Core read-model/queue/proxy mechanics stand, but the original SSO-dead claim is false, B36 is now resolved/refined, and I found two additional dashboard defects.

### Adjudication (each: CONFIRM/REFUTE/REFINE + path:line)
- CONFIRM §A14/verifier: original "SSO is dead" is false. Docker provisions a per-user SSO secret (`bin/arclink-docker.sh:1672`, `bin/arclink-docker.sh:1696`, `bin/arclink-docker.sh:1895`), installer writes `sso_session_secret`/`sso_subject` (`bin/install-deployment-hermes-home.sh:92`, `bin/install-deployment-hermes-home.sh:163`), and proxy accepts it (`python/arclink_dashboard_auth_proxy.py:674`, `python/arclink_dashboard_auth_proxy.py:701`, `python/arclink_dashboard_auth_proxy.py:735`).
- CONFIRM §A14/verifier: `--agent-title` exists but in-repo enrollment callers do not send it (`python/arclink_headless_hermes_setup.py:638`, `python/arclink_enrollment_provisioner.py:1409`, `python/arclink_enrollment_provisioner.py:1579`).
- REFINE §B36: shared-host `arclink-web-access.json` port producer is `ensure_access_state`, which emits exact `dashboard_backend_port`/`dashboard_proxy_port` keys (`python/arclink_agent_access.py:517`); `install-agent-user-services.sh` hard-subscripts those keys and fails open through `eval "$(python ...)"` semantics (`bin/install-agent-user-services.sh:276`, `bin/install-agent-user-services.sh:285`, `bin/install-agent-user-services.sh:416`, `bin/install-agent-user-services.sh:434`). Docker installer writes auth/SSO access state but does not create fresh port keys except by preserving `**existing` (`bin/install-deployment-hermes-home.sh:163`).
- REFINE §B36: no CANON-22 backup script consumes `backup_deploy_key_private_ref`; the action worker only records `failed_closed` (`python/arclink_action_worker.py:954`, `python/arclink_action_worker.py:973`), while shipped agent backup uses separate `arclink-agent-backup.env` and `AGENT_BACKUP_KEY_PATH` (`bin/backup-agent-home.sh:18`, `bin/backup-agent-home.sh:39`, `bin/configure-agent-backup.sh:239`).
- CONFIRM MEDIUM: `_token_secret` silently falls back to `sha256(realm/user/password)` when `session_secret` is absent (`python/arclink_dashboard_auth_proxy.py:86`); current producers do set a real `session_secret` (`python/arclink_agent_access.py:479`, `bin/install-deployment-hermes-home.sh:90`).
- CONFIRM MEDIUM: backup deploy-key staging persists a private ed25519 key server-side (`python/arclink_dashboard.py:1090`, `python/arclink_dashboard.py:1104`) and stores only a `server_state:` private ref in deployment metadata (`python/arclink_dashboard.py:1231`, `python/arclink_dashboard.py:1237`).
- REFINE MEDIUM: scale snapshot env coupling is broader than stated: `build_scale_operations_snapshot` calls readiness helpers without env injection and reads `os.environ` for rollout/planner/strategy (`python/arclink_dashboard.py:701`, `python/arclink_dashboard.py:705`, `python/arclink_dashboard.py:715`, `python/arclink_dashboard.py:748`).
- CONFIRM MEDIUM: two CANON-19 paths mutate the same `config.yaml` with different strategies: full Hermes config load/save (`python/arclink_headless_hermes_setup.py:565`, `python/arclink_headless_hermes_setup.py:599`) vs byte-preserving line surgery (`python/arclink_skill_enablement.py:118`, `python/arclink_skill_enablement.py:264`).
- CONFIRM/REFINE seam CANON-23→19: dashboard’s blocker check ignores `CLOUDFLARE_API_TOKEN_REF` alternates because it directly checks `step.required_env` against `env_source`; live-journey credential code does know the alternate (`python/arclink_dashboard.py:544`, `python/arclink_dashboard.py:548`, `python/arclink_live_journey.py:53`, `python/arclink_live_journey.py:69`).

### New findings both Claude passes missed (severity + path:line)
- MEDIUM: user dashboard hardcodes provider display/evaluation to default Chutes. `read_arclink_user_dashboard` calls `primary_provider({})`, so `env_value` reads an empty mapping and returns default `ARCLINK_PRIMARY_PROVIDER="chutes"`; the dashboard then runs Chutes boundary enrichment even for Codex/Anthropic provider setups (`python/arclink_dashboard.py:1737`, `python/arclink_dashboard.py:1743`, `python/arclink_product.py:14`, `python/arclink_product.py:49`, `python/arclink_product.py:87`, `python/arclink_onboarding_provider_auth.py:126`, `python/arclink_onboarding_provider_auth.py:139`).
- LOW: `queue_arclink_admin_action` is SELECT-then-INSERT for idempotency; the unique index prevents duplicates, but a concurrent same-key replay can raise `IntegrityError` instead of returning the existing row (`python/arclink_dashboard.py:2359`, `python/arclink_dashboard.py:2376`, `python/arclink_control.py:2269`).

### Claude citations re-confirmed or corrected
- Reconfirmed: user/admin dashboard API gates and read-model returns (`python/arclink_api_auth.py:1177`, `python/arclink_api_auth.py:1181`, `python/arclink_api_auth.py:1649`, `python/arclink_dashboard.py:1797`, `python/arclink_dashboard.py:2299`).
- Reconfirmed: admin action queue validation, queued-row producer, audit, and worker CAS consumer (`python/arclink_dashboard.py:2339`, `python/arclink_dashboard.py:2376`, `python/arclink_dashboard.py:2396`, `python/arclink_action_worker.py:453`, `python/arclink_action_worker.py:474`).
- Corrected: `_safe_json` itself calls `json_dumps_safe`; `json_dumps_safe` calls `reject_secret_material` (`python/arclink_dashboard.py:777`, `python/arclink_boundary.py:65`, `python/arclink_boundary.py:72`).
- Reconfirmed: backup write-check name lies; dashboard and worker both record `failed_closed`, no git (`python/arclink_dashboard.py:1379`, `python/arclink_action_worker.py:973`).
- Reconfirmed: auth proxy fail-closed load, signed token checks, CSRF gate, backend token injection after auth (`python/arclink_dashboard_auth_proxy.py:173`, `python/arclink_dashboard_auth_proxy.py:621`, `python/arclink_dashboard_auth_proxy.py:1104`, `python/arclink_dashboard_auth_proxy.py:1176`, `python/arclink_dashboard_auth_proxy.py:1221`).

### Residual disagreement with the Claude half (for final reconciliation)
- No disagreement with the verifier’s SSO correction. I object to the original record’s "SSO dormant/INFO" framing and add the provider-hardcode and idempotency-race findings above.
- Severity: live SSO is MEDIUM in this codebase, not INFO; I do not raise it to HIGH without a proof that sibling dashboards cross a stronger tenant boundary than same Captain/user (`bin/arclink-docker.sh:1672`, `python/arclink_dashboard_auth_proxy.py:735`).
<<<CODEX-VERDICT-END CANON-19>>>
