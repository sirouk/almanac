<<<CODEX-VERDICT-START CANON-26>>>
## CANON-26 — Codex (GPT-5.5 xhigh) ratification
SIGN-OFF: OBJECT(3)
ONE-LINE VERDICT: The unit map is sound, but the safety record needs correction: access-state parsing fails open, the agent unit renderer is injectable, and SSH-key install has a missed newline-injection bypass.

### Adjudication (each: CONFIRM/REFUTE/REFINE + path:line)
- REFINE: static inventory is 29 user units, but the split is 19 `.service` / 10 `.timer`, not 18/11; the extra unpaired service is `arclink-pdf-ingest-watch.service` and it is always disabled by the installer. `systemd/user/arclink-pdf-ingest-watch.service:6`, `bin/install-user-services.sh:72`.
- CONFIRM: static units are thin `%h/arclink/bin/*` wrappers; representative services include MCP, memory-synth, qmd-update, and pdf-watch. `systemd/user/arclink-mcp.service:6`, `systemd/user/arclink-memory-synth.service:6`, `systemd/user/arclink-qmd-update.service:6`, `systemd/user/arclink-pdf-ingest-watch.service:6`.
- CONFIRM: `install-user-services.sh` copies user units, gates on the user D-Bus, enables the fixed timer/service set, and can abort half-enabled under `set -e` on enable/restart. `bin/install-user-services.sh:9-12`, `bin/install-user-services.sh:30-34`, `bin/install-user-services.sh:49-59`, `bin/install-user-services.sh:109-120`.
- CONFIRM: generated system units are root-only, guarded before interpolation, and render enrollment + claims timers with `--claims-only`. `bin/install-system-services.sh:8`, `bin/install-system-services.sh:45-48`, `bin/install-system-services.sh:50-87`, `python/arclink_enrollment_provisioner.py:145`, `python/arclink_enrollment_provisioner.py:3336-3338`.
- CONFIRM: cross-piece exec seams hold for memory, notification, health, dashboard proxy, qmd, curator, and backup. `bin/memory-synth.sh:32`, `python/arclink_memory_synthesizer.py:1920-1927`, `systemd/user/arclink-notification-delivery.timer:5-8`, `python/arclink_notification_delivery.py:1947-1955`, `bin/health-watch.sh:21`, `python/arclink_dashboard_auth_proxy.py:1367-1371`, `bin/qmd-refresh.sh:125-126`, `bin/arclink-curator-onboarding.sh:9-10`, `bin/backup-agent-home.sh:8-17`.
- CONFIRM: CANON-18 -> CANON-26 ssot-batcher wake seam is real. `python/arclink_notion_webhook.py:42-49`.
- CONFIRM/HIGH: `arclink-web-access.json` parsing fails open, not closed; `enable_access_surfaces=1` is set before `eval "$(python3 ...)"`, KeyError/JSON errors leave empty ports, then dashboard/proxy units render and enable/restart. `bin/install-agent-user-services.sh:276-294`, `bin/install-agent-user-services.sh:394-416`, `bin/install-agent-user-services.sh:424-434`, `bin/install-agent-user-services.sh:474-478`.
- CONFIRM/MEDIUM: agent unit directive injection is real; guard runs only through `systemd_env_line`, while `Description=`, `WorkingDirectory=`, `PathChanged=`, and `ExecStart=` interpolate raw values, and heredoc command-substitution exits do not abort the parent render. `bin/install-agent-user-services.sh:47-51`, `bin/install-agent-user-services.sh:298`, `bin/install-agent-user-services.sh:345-348`, `bin/install-agent-user-services.sh:380`, `bin/install-agent-user-services.sh:416`, `bin/install-agent-user-services.sh:434`.
- REFINE: access-state producer is verified for enrolled-user systemd path: provisioner calls `ensure_access_state`, which writes exact local port keys. The same filename is schema-overloaded by deployment-home install, which writes dashboard URLs/SSO fields but no backend/proxy ports. `python/arclink_enrollment_provisioner.py:1030-1037`, `python/arclink_agent_access.py:517-526`, `python/arclink_agent_access.py:551`, `bin/install-deployment-hermes-home.sh:163-175`.
- REFINE: `disable_native_hermes_gateway_units` only enumerates `hermes-gateway*`; public repo evidence does not prove CANON-30 native unit naming completeness. `bin/install-agent-user-services.sh:250-269`.
- REFINE: per-agent gateway emits `gateway run --replace` in systemd and Docker helper, while Curator emits bare `gateway`; `hermes-shell.sh` only forwards to runtime Hermes, so the Hermes CLI contract remains not both-ends-verified from this repo. `bin/install-agent-user-services.sh:383`, `python/arclink_agent_process_helper.py:705-707`, `bin/curator-gateway.sh:162`, `bin/hermes-shell.sh:12`.

### New findings both Claude passes missed (severity + path:line)
- HIGH: remote SSH public-key validation allows an internal newline after a no-comment first key, and the installer appends the whole value, producing a second unrestricted `authorized_keys` line that bypasses the intended `from=...`, no-forwarding restrictions. `python/arclink_onboarding_flow.py:76-77`, `python/arclink_onboarding_flow.py:955`, `python/arclink_onboarding_flow.py:995`, `bin/install-agent-ssh-key.sh:54-56`, `bin/install-agent-ssh-key.sh:73-74`, `bin/install-agent-ssh-key.sh:115`.
- LOW: `ARCLINK_AGENT_REMOTE_SSH_FROM` is interpolated inside `from="..."` with no quote/control validation, so operator-side misconfiguration can inject additional authorized_keys options. `bin/install-agent-ssh-key.sh:72-74`.

### Claude citations re-confirmed or corrected
- Corrected: service/timer split is 19/10, access-state risk is fail-open, and the agent renderer injection guard is not a strength. `systemd/user/arclink-pdf-ingest-watch.service:6`, `bin/install-agent-user-services.sh:276-294`, `bin/install-agent-user-services.sh:47-51`.
- Reconfirmed: system renderer guard is sound because it rejects config/repo values before heredocs and uses static descriptions. `bin/install-system-services.sh:18-30`, `bin/install-system-services.sh:45-48`, `bin/install-system-services.sh:50-87`.
- Reconfirmed: Hermes cron installer is not systemd; it writes native Hermes cron state and a script. `bin/install-agent-cron-jobs.sh:6-10`, `bin/install-agent-cron-jobs.sh:348-363`, `bin/install-agent-cron-jobs.sh:406-433`.
- Reconfirmed: dead/legacy units are deliberate but confusing. `bin/install-user-services.sh:72`, `bin/install-agent-user-services.sh:442`, `bin/install-agent-user-services.sh:479`.

### Residual disagreement with the Claude half (for final reconciliation)
- No residual disagreement with the Claude verifier on CANON-26 HIGH/MEDIUM findings; I add the SSH-key newline injection as an additional HIGH.
- The original Claude record should not be ratified as written because its “fail-closed KeyError” and “injection guard strength” claims are materially wrong. `bin/install-agent-user-services.sh:276-294`, `bin/install-agent-user-services.sh:47-51`.
- CANON-30 Hermes gateway CLI compatibility remains open from public-repo evidence; ArcLink emits two invocation styles. `bin/install-agent-user-services.sh:383`, `bin/curator-gateway.sh:162`.
<<<CODEX-VERDICT-END CANON-26>>>
