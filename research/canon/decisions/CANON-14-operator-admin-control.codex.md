<<<CODEX-DECISIONS-START CANON-14>>>
## CANON-14 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: Do not let `academy_apply` materialize with PG-HERMES only
- RECOMMENDATION: Keep the current fail-closed production gate. `stage_academy_apply` should require `live_adapter + live_authorized + review_ready + trainer_review_ready + provider_review_ready` before `writes_enabled=True` ([python/arclink_academy_programs.py](/root/arclink/python/arclink_academy_programs.py:3015)), with `provider_review_ready` derived from `live_enrichment_status == "live_reviewed"` ([python/arclink_academy_programs.py](/root/arclink/python/arclink_academy_programs.py:2966)). Resolve the failing test by making the fixture establish the exact central specialist’s PG-PROVIDER review via `run_academy_trainer_review(..., live_authorized=True, client=<test live client>)` before expecting Hermes-home writes, and keep/add the negative assertion that PG-HERMES alone returns `failed_closed` ([tests/test_arclink_academy_programs.py](/root/arclink/tests/test_arclink_academy_programs.py:621)). Treat this as CANON-17 Academy contract alignment, not a CANON-14 operator-admin relaxation.
- SYMPHONY ANCHOR: **Academy Trainer And Subject-Matter Formation**: “Agent mutation remain under ... PG-PROVIDER, and PG-HERMES gates.” Also **North Star proof rule**: “Every step should have ... a named live proof gate where external systems are required.”
- RATIONALE: This keeps the operator-owned policy boundary intact: Captains can train and stage, but Agent-home mutation waits for named PG-PROVIDER proof plus PG-HERMES authorization. The action worker already passes adapter and `ARCLINK_ACADEMY_APPLY_LIVE` into the Academy owner ([python/arclink_action_worker.py](/root/arclink/python/arclink_action_worker.py:1155)) and materializes only when `writes_enabled` is true ([python/arclink_action_worker.py](/root/arclink/python/arclink_action_worker.py:2148)). That preserves Pod state by default, prevents cosmetic proof-gates, and leaves the same truth visible in result fields like `academy_provider_review_ready`, `academy_trainer_live_status`, and `proof_gates`.
- TRADEOFFS & ALTERNATIVES: Rejected relaxing the gate to PG-HERMES-only because it makes PG-PROVIDER advisory while the surface still advertises it. Rejected skipping/xfailing the test because that hides contract drift. Rejected real provider calls in unit tests because they are secret-gated and flaky; local tests should use a synthetic live Trainer, while live provider availability belongs to the named PG-PROVIDER live-proof lane.
- EFFORT / BLAST-RADIUS: low. Touches Academy/action-worker regression fixtures and assertions only unless the current branch lacks the fail-closed PG-PROVIDER gate; no operator-admin production queue behavior should change.

<<<CODEX-DECISIONS-END CANON-14>>>
