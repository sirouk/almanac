<<<CODEX-DECISIONS-START CANON-16>>>
## CANON-16 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: Budget fail-open provenance for `observe_only_unlimited`
- RECOMMENDATION: Keep the Operator Pod metered-but-unlimited, but make `observe_only_unlimited` fail closed unless DB provenance proves this is the configured single Operator identity. Do not trust `arclink_deployments.metadata_json` alone. Use `arclink_operator_agent.py` settings (`operator_agent_deployment_id`, `operator_agent_user_id`) plus the single-operator metadata invariant as the source owner; do not hard-code `deployment_id == "operator"`. Code-level: add an `observe_unlimited_authorized` decision helper and pass it into `evaluate_chutes_deployment_boundary(...)` from router/provider-state/usage-ingest call sites. If unauthorized, return `403 budget_policy_not_authorized`, do not call upstream, preserve metadata, and write a redacted `llm_router:budget_policy_rejected` event. Keep `used_cents` preserved for the authorized Operator lane. Add local regressions for authorized Operator success, Captain metadata spoof rejection, no upstream call on rejection, same provider-state truth, and redacted evidence. Live Chutes behavior remains `PG-PROVIDER`.
- SYMPHONY ANCHOR: `Inference And Router Policy` — “letting the Operator remain effectively unlimited but still observable.” `Identity, Access, And Session Governance` — “Admin and Operator actions should never trust client-asserted privilege... without server-side authorization.”
- RATIONALE: This preserves the intentional Operator recovery lane while closing the real tenant-policy escape. The Operator owns host policy; Captains own Pods, not the host. The canonical source becomes the operator-agent identity row/settings, not mutable deployment metadata. Unauthorized unlimited policy fails before inference, leaves state intact, and creates redacted evidence.
- TRADEOFFS & ALTERNATIVES: Rejecting unlimited entirely would contradict the symphony’s Operator remediation lane. Trusting metadata is too weak. Hard-coding `operator` would break configurable operator identities. Residual risk is host/DB compromise, which is already inside the Operator trust boundary.
- EFFORT / BLAST-RADIUS: med — touches `python/arclink_operator_agent.py`, `python/arclink_chutes.py`, `python/arclink_llm_router.py`, provider-state usage callers, docs, and focused router/operator tests.

### DECISION 2: Mid-stream error SSE after valid chunks
- RECOMMENDATION: Ratify the current v1 wire contract: after any valid stream chunks have been yielded, the router must not replay fallback; it should append one terminal `data:` SSE frame containing `{"error": {...}, "arclink_router": {..."streaming_fallback":"unavailable_after_stream_started"...}}`, then close without pretending success. Do not change this default to `event: error` or close-only in-place. Strengthen tests around `python/arclink_llm_router.py:1813-1855` to assert chunk order, terminal error frame shape, no `[DONE]` after router failure, failed usage settlement, and no prompt/chunk/secret persistence. Document it as an ArcLink OpenAI-compatible extension; if strict-client compatibility is needed later, add a negotiated new mode/header rather than mutating v1.
- SYMPHONY ANCHOR: `Inference And Router Policy` — “once a stream has started, the router labels fallback as unavailable instead of replaying a partially delivered request.” `Notifications, Incidents, And Evidence` — “ArcLink should never fail silently.”
- RATIONALE: HTTP status is already committed once stream chunks are sent, so in-band SSE is the only reliable client-visible failure signal. The current shape is explicit, sanitized, auditable, and state-preserving: clients see the partial failure, the router records failed usage, and no raw prompt/completion content is stored.
- TRADEOFFS & ALTERNATIVES: Close-only is more OpenAI-pure but can look like a successful truncated stream to weak clients. `event: error` is more SSE-native but is a breaking wire-shape change for existing data-frame parsers. Replaying fallback after chunks is rejected because it risks duplicate partial answers and confused billing/evidence.
- EFFORT / BLAST-RADIUS: low to med — mostly router streaming tests/docs/client contract; no schema change, but it fixes a public wire contract that API clients may depend on.

<<<CODEX-DECISIONS-END CANON-16>>>
