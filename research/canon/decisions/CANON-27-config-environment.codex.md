<<<CODEX-DECISIONS-START CANON-27>>>
## CANON-27 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: `team-resources.example.tsv` is pipe-delimited despite the `.tsv` name
- RECOMMENDATION: Keep `config/team-resources.example.tsv` and the private default `arclink-priv/config/team-resources.tsv` as the stable v1 operator contract. Do not rename or switch delimiters in place. Make the legacy filename explicit in comments/docs, and add a `bin/clone-team-resources.sh --check` preflight that validates non-comment lines before sudo/git work: expected pipe syntax, slug + URL required, branch/note optional per current defaults, tab-delimited lines rejected with redacted line numbers. Add `tests/test_arclink_repo_sync.py` coverage for valid pipe manifests and malformed tab manifests failing before git.
- SYMPHONY ANCHOR: Configuration, Schema, And Migration: "`private files they must preserve`"; Governance And Proof: "`A claim is local-real only when source and regression tests prove it.`"
- RATIONALE: Current code already makes this an operator contract: the example says `slug|git-url|branch|note`, the script defaults to `arclink-priv/config/team-resources.tsv`, and parsing is `IFS='|'`. Existing repo-sync tests also name `team-resources.tsv`. Preserving the filename protects private operator state; adding a local check makes the actual delimiter fail closed and test-proven.
- TRADEOFFS & ALTERNATIVES: Renaming to `.psv` is cleaner but breaks public docs, private manifests, overrides, and tests. Supporting both `.tsv` and `.psv` creates two truths. Silent compatibility without validation keeps the footgun. Residual risk: the name stays imperfect, but the contract becomes explicit and locally enforced.
- EFFORT / BLAST-RADIUS: low; touches `config/team-resources.example.tsv`, `bin/clone-team-resources.sh`, `docs/org-profile.md`, and `tests/test_arclink_repo_sync.py`.

### DECISION 2: Do not derive every Docker-authority inventory field from Compose without CANON-12/25 owner agreement
- RECOMMENDATION: Do not expand the drift test to mechanically derive every structured/policy field from Compose. Instead, add a field-ownership matrix in `tests/test_arclink_docker.py`: Compose-owned fields are parsed and compared (`docker_socket`, `explicit_root`, `container_user` where explicit, capabilities, service networks, default/internal/egress network classification, broad env/secret/private mount presence where parseable); inventory/operator-owned fields remain explicit policy evidence (`authority_class`, purpose, `why_socket_needed`, `trusted_host_risk_acceptance_gate`, child-process allowlist, residual state, GAP review/control records). Fix the current overstatement by deriving `container_user` from `user:` instead of literals where possible, and keep targeted tests like `test_operator_upgrade_broker_compose_boundary_minimizes_env_and_private_mounts`.
- SYMPHONY ANCHOR: Governance And Proof: "`A policy choice stays a policy question until the operator/product decision is recorded and tests encode it.`" Also: "`A residual risk stays visible until removed or explicitly accepted.`"
- RATIONALE: Compose is the runtime source for mounts, users, caps, env, and networks; it is not the owner of the threat model or residual-risk acceptance. The current parser is text-based and currently derives only socket/root/caps/networks plus default-network, while policy fields are asserted separately. Keeping source ownership split preserves same-truth behavior without pretending Compose can author operator policy.
- TRADEOFFS & ALTERNATIVES: Full derivation sounds rigorous but would cross CANON-12/25 boundaries, flatten human review fields into brittle parser guesses, and risk false confidence. Leaving tests as-is keeps the known gap where `container_user` is not truly parsed. The recommended middle path increases local proof for objective fields while keeping residual-risk and acceptance gates operator-owned.
- EFFORT / BLAST-RADIUS: med; touches `tests/test_arclink_docker.py`, `config/docker-authority-inventory.json` if stale prose/field ownership labels are updated, and any CANON-12/25 docs that define Docker authority ownership.
<<<CODEX-DECISIONS-END CANON-27>>>
