<<<CODEX-DECISIONS-START CANON-28>>>
## CANON-28 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: Orphaned test functions and repository-wide self-executing test contract
- RECOMMENDATION: Approve this as a CANON-29 test-corpus contract, then wire it into CANON-28. Keep the current direct runner in `.github/workflows/install-smoke.yml:30-42`, but add a source-owned verifier such as `bin/check-test-entrypoints.py` that fails closed unless every `tests/test_*.py` has a valid `__main__` path: either `unittest.main(...)`, or a `main()` that directly or explicitly-list-runs every top-level `test_*` function. Run it before the direct test loop and cover it with fixture tests. Do not switch to pytest as a side effect.
- SYMPHONY ANCHOR: Whole-System Traversal: “Every step should have a local source owner, a local regression or dry-run proof...” (`docs/arclink/sovereign-control-node-symphony.md:158-161`).
- RATIONALE: CANON-28 owns the CI producer; CANON-29 owns test corpus semantics. The code currently depends on executable scripts, not pytest (`.github/workflows/install-smoke.yml:35-42`; `docs/arclink/local-validation.md:14-17`). Some files use explicit test lists (`tests/test_deploy_regressions.py:4603-4745`), while others use `unittest.main` (`tests/test_arclink_evidence.py:268-269`), so a naïve “direct call graph only” gate would reject valid runners. A formal verifier makes the contract local, explicit, and fail-closed.
- TRADEOFFS & ALTERNATIVES: Rejected immediate pytest migration because it changes the established execution model and could mask script-runner assumptions. Rejected a broad grep-only call graph because it misses list-driven runners and unittest. Residual risk: AST validation must stay conservative and may require explicit allowlist metadata for unusual harnesses.
- EFFORT / BLAST-RADIUS: high; touches CI, test harness conventions, CANON-29 docs/tests, and possibly many `tests/test_*.py` files, but not runtime behavior.

### DECISION 2: Provider-auth/provider-unavailable live-smoke skip exits 0
- RECOMMENDATION: Split “maintenance completed” from “PG-PROVIDER passed.” Change `bin/live-agent-tool-smoke.sh` so provider auth failure/unavailability produce structured `blocked` results for `PG-PROVIDER` and non-zero exit by default, instead of `skipped` exit 0 (`bin/live-agent-tool-smoke.sh:417-440`, `:481-489`). Add an explicit deploy wrapper/flag for ordinary install/upgrade best-effort behavior: deploy may continue after host health, but it must record/print redacted evidence that `PG-PROVIDER` is blocked, not passed. Add strict mode, e.g. `ARCLINK_REQUIRE_LIVE_AGENT_TOOL_SMOKE=1`, where the same blocked provider result aborts.
- SYMPHONY ANCHOR: Notifications, Incidents, And Evidence: “A clear split between local dry-run proof, authorized live proof, policy decision...” (`docs/arclink/sovereign-control-node-symphony.md:996-997`).
- RATIONALE: Blocking all upgrades on a provider outage fights the operator-owned upgrade/repair model, but returning 0 from the proof script lets surfaces confuse “not proven” with “passed.” The current install/upgrade path runs the smoke after strict health (`bin/deploy.sh:5606-5618`, `:5743-5755`); that is the right place to preserve state, finish the host operation, and leave a same-truth proof status. Provider credentials/status should fail closed at the provider gate, not silently become a green deploy.
- TRADEOFFS & ALTERNATIVES: Rejected “always fail deploy on provider outage” because it can strand urgent security or repair upgrades behind an external system. Rejected “keep exit 0” because it violates proof-gate honesty. Residual risk: dashboards/Raven must consume the new blocked evidence or operators will still rely on console output.
- EFFORT / BLAST-RADIUS: med; touches `live-agent-tool-smoke.sh`, deploy/docker wrappers, deploy regressions, and evidence/status surfaces.

### DECISION 3: Full ruff/pyflakes style enforcement
- RECOMMENDATION: Keep the current fatal lint gate as mandatory (`.github/workflows/install-smoke.yml:27-28`), but do not flip on full repo-wide ruff/pyflakes as one blocking switch. Instead, make lint policy source-owned: add `ruff.toml` or `pyproject.toml`, define the intended rule set, and add a baseline/ratchet wrapper that fails on any new unbaselined finding. Move toward Ruff as the single enforced engine, since Ruff covers Pyflakes-style `F` rules; either remove `pyflakes` from required deps after parity or keep it only as a documented optional cross-check. Once the baseline is empty, replace the ratchet with strict full enforcement.
- SYMPHONY ANCHOR: Supply Chain, Build, And Release Integrity: “Python dependencies... shell scripts... built from known source and validated before deployment.” (`docs/arclink/sovereign-control-node-symphony.md:1242-1245`).
- RATIONALE: The repair already added a fail-closed fatal lint gate, while `requirements-dev.txt:12-13` still pins both `pyflakes` and `ruff`, and `bin/ci-preflight.sh:197-202` only compiles three Python modules. A ratchet gives operators policy ownership and prevents new lint debt without turning unrelated cross-piece cleanup into an upgrade blocker. It also keeps local docs, CI, and preflight aligned on the same lint truth.
- TRADEOFFS & ALTERNATIVES: Rejected immediate strict full lint because existing warnings would force a broad cleanup unrelated to CANON-28. Rejected leaving only fatal lint forever because it under-validates release integrity. Residual risk: baselines can become permanent unless CI reports remaining counts and cleanup is tracked.
- EFFORT / BLAST-RADIUS: med now, high to reach zero baseline; touches CI, dev dependencies, validation docs, and eventually broad Python style cleanup.

<<<CODEX-DECISIONS-END CANON-28>>>
