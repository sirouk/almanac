<<<CODEX-DECISIONS-START CANON-29>>>
## CANON-29 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: Whether CANON-29 requires literal dedicated `test_arclink_upgrade_policy.py` and `test_arclink_rejection_incidents.py` files
- RECOMMENDATION: Do not block on the literal filenames. Accept the existing behavioral coverage in the owning runnable files, then update the CANON-29 repair status from `needs-decision` to resolved: “dedicated filenames waived; direct-runner behavioral coverage accepted.” Current source already covers `arclink_upgrade_policy` directly in `tests/test_arclink_operator_raven.py:324` and wires it at `tests/test_arclink_operator_raven.py:1191`; it covers rejection incidents in `tests/test_arclink_notification_delivery.py:1757` and `:1933`, wired at `:2613-2614`. Keep the direct-runner orphan guard in `tests/test_documentation_truths.py:458-472` as the canonical corpus contract, because CI runs `python3 tests/test_*.py` directly via `.github/workflows/install-smoke.yml:35-42`.
- SYMPHONY ANCHOR: Governance And Proof: “A claim is local-real only when source and regression tests prove it.”
- RATIONALE: The symphony optimizes for source ownership and enforced proof, not test-file taxonomy. `upgrade_policy` is source-owned in `python/arclink_upgrade_policy.py:270-301`, Raven renders it read-only/fail-closed in `python/arclink_operator_raven.py:1199-1245`, and tests assert read-only summaries, aliases, proof gates, and unknown-component failure. Rejection incidents are source-owned in `python/arclink_rejection_incidents.py:128-161`, called by the gateway broker before subprocess work in `python/arclink_gateway_exec_broker.py:151-161` and `:287-293`, and tests assert redaction, safe paths, permissions, and symlink refusal. That satisfies local proof, fail-closed behavior, secret redaction, and same-truth runner enforcement.
- TRADEOFFS & ALTERNATIVES: Dedicated files would improve browsing, but they add no stronger gate unless wired into the same direct CI runner. Requiring them after the workspace refused writes would turn an already-covered behavior into a paperwork blocker. A later cleanup may split these tests into dedicated files if writable, but only if the direct-runner guard stays green. Residual risk: this read-only host lacks a usable temp directory, so the two tempdir-heavy files cannot be executed here end-to-end; the pure corpus guard passed.
- EFFORT / BLAST-RADIUS: low; touches only CANON-29 repair/status docs, with optional future test-file reorganization. No production code, schema, API, or threat-model change required.

<<<CODEX-DECISIONS-END CANON-29>>>
