<<<CODEX-DECISIONS-START CANON-30>>>
## CANON-30 — Codex (GPT-5.5 xhigh) decision recommendations (symphony-anchored)

### DECISION 1: Replace installer regex YAML edits with a comment-preserving parser
- RECOMMENDATION: Adopt a real round-trip YAML editor using `ruamel.yaml`, not PyYAML, and move the four heredoc editors in `bin/install-arclink-plugins.sh:64-173`, `:183-267`, `:275-378`, and `:386-462` into one source-owned helper, e.g. `python/arclink_hermes_config.py`. The helper should preserve comments/order/anchors, update only `plugins.enabled`, `plugins.disabled`, `dashboard.theme`, and `dashboard.hidden_plugins`, take the existing backup from `bin/install-arclink-plugins.sh:53-58`, use the existing `.arclink-config.yaml.lock` convention, write atomically, and fail closed with a clear dependency/parse error if round-trip parsing is unavailable. Pin the dependency in the runtime/dev lanes (`Dockerfile:88-92`, `requirements-dev.txt:5`) and install it through bootstrap/runtime dependency paths, not ad hoc inside the installer.
- SYMPHONY ANCHOR: `Configuration, Schema, And Migration` — “config/state files they own and which private files they must preserve”; `Supply Chain, Build, And Release Integrity` — “Python dependencies ... built from known source and validated before deployment.”
- RATIONALE: Current code is still indentation/regex surgery and `write_text` based, even after backups. The tests at `tests/test_arclink_plugins.py:320-361` and `:366-398` already define the right contract: comments and future nested config must survive. A round-trip parser makes `config.yaml` a versioned local source-owned surface, keeps Hermes surfaces in release lock-step, and fails closed instead of silently corrupting user/operator state.
- TRADEOFFS & ALTERNATIVES: Reject PyYAML because it drops comments and would violate the current regression contract. Reject keeping regex plus more edge cases because tabs, anchors, flow style, and future nested structures keep expanding the parser surface. Residual risk is a new dependency in host/runtime install lanes; mitigate with pinning, dependency preflight, and a local fixture test for “missing parser fails with clear upgrade requirement.”
- EFFORT / BLAST-RADIUS: med. Touches `bin/install-arclink-plugins.sh`, a new Python helper/module, runtime/dev dependency manifests, bootstrap dependency checks, and focused plugin installer tests.

### DECISION 2: Redesign Drive path safety around root policy plus fd-anchored operations
- RECOMMENDATION: Do not make filename denylisting the security boundary. Define Drive’s boundary as an operator/provisioner-owned allowlist of exposed roots and capabilities, then enforce access with fd-anchored filesystem helpers. Keep the current secret-name checks at `plugins/hermes-agent/drive/dashboard/plugin_api.py:589-614` as defense in depth, but move authorization away from `resolve(strict=False)` checks in `:1168-1190`. Implement helpers using `openat`/`dir_fd`, `O_NOFOLLOW`, `fstat`, and atomic `renameat`/`replace` equivalents for read, preview, download, upload, mkdir, move, copy, trash, and restore paths currently handled around `:1347-1461`, `:1916-2009`, and `:2012-2185`. Linked roots should only cross into manifest-approved `source_path` entries, then become a new anchored root; arbitrary symlinks remain rejected.
- SYMPHONY ANCHOR: `Pods, Isolation, And SOUL` — “Pods cannot read or write another Captain's state”; `Abuse, Safety, And Platform Boundaries` — “Upload/file safeguards for ... symlink, path escape ... cases in Drive.”
- RATIONALE: Captains own Pod workspace contents, not host state or another Captain’s state. Current Drive behavior is useful and should not be narrowed to a broad content allowlist, but the enforcement must fail closed under races. Root allowlisting makes operator policy explicit; fd anchoring closes TOCTOU gaps; redacted 403/409 evidence preserves state and keeps Drive/Code/Terminal truth aligned.
- TRADEOFFS & ALTERNATIVES: A strict file-extension allowlist would reduce risk but break legitimate file-manager and code-project workflows. A bigger denylist would still miss unknown secret names. Pure fd anchoring without root policy would close symlink races but still rely on ambient env roots. Residual risk: secrets deliberately stored inside an exposed workspace are visible to that Pod owner; that should be documented as policy, with secrets kept in private state/secrets paths.
- EFFORT / BLAST-RADIUS: high. Touches Drive’s backend path layer, linked-share semantics, upload/trash/copy/move/restore behavior, and likely shared root-policy tests so Drive and Code do not present conflicting filesystem truth.
<<<CODEX-DECISIONS-END CANON-30>>>
