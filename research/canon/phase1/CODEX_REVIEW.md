<<<CODEX-REVIEW-START>>>
SIGN-OFF: OBJECT(3)

### 1. Outage fix
REFINE/OBJECT: default-OFF now avoids the shipped outage: the gate reads `os.environ["ARCLINK_BRIDGE_GETME_CACHE"]` with default `0`, and flag-off emits the legacy bridge command (`python/arclink_notification_delivery.py:695`, `python/arclink_notification_delivery.py:700`). The gateway image really is the dependency: repo code is baked by `COPY .` (`Dockerfile:72`), the image defaults to `USER arclink` (`Dockerfile:117`), and the operator gateway only mounts operator state, not the repo (`compose.yaml:381`, `compose.yaml:388`).

REFUTE “fully closes”: flag ON still emits `-u 0:0 ... arclink_public_agent_bridge_root.py` (`python/arclink_notification_delivery.py:702`, `python/arclink_notification_delivery.py:710`). If that script is missing in the gateway image, there is no preflight or legacy fallback; sync execution returns bridge failure (`python/arclink_notification_delivery.py:1017`), detached execution marks the row failed (`python/arclink_notification_delivery.py:1569`), and the broker returns only `exit status` (`python/arclink_gateway_exec_broker.py:290`). A preflight that the wrapper exists in the target gateway image is needed before enabling L2; an auto-fallback is safe only for unmistakable “wrapper file missing before child ran,” not arbitrary wrapper failure.

REFINE on `os.environ`: it is the right lock-step source for avoiding “config edited but container not recreated” skew, but the current trusted-host broker path does not reliably carry the flag. `notification-delivery` defaults to the gateway exec broker (`compose.yaml:1045`, `python/arclink_notification_delivery.py:959`), and the broker reconstructs the command itself (`python/arclink_gateway_exec_broker.py:218`, `python/arclink_gateway_exec_broker.py:242`), but broker env omits `ARCLINK_BRIDGE_GETME_CACHE` (`compose.yaml:1015`). Result: L2 is likely silently disabled on the main broker path.

### 2. Root wrapper security
CONFIRM with precision: a chat-triggered wrapper process can run as root, but the Hermes agent child is dropped before execution. The wrapper resolves a nonzero runtime uid/gid (`python/arclink_public_agent_bridge_root.py:34`), drops groups/gid/uid (`python/arclink_public_agent_bridge_root.py:55`), and passes that as `preexec_fn` to the child bridge (`python/arclink_public_agent_bridge_root.py:119`, `python/arclink_public_agent_bridge_root.py:127`).

CONFIRM validator scope: `-u 0:0` is accepted only when the tail is exactly `python arclink_public_agent_bridge_root.py` (`python/arclink_notification_delivery.py:612`, `python/arclink_notification_delivery.py:616`, `python/arclink_notification_delivery.py:620`), and the compose form has the same wrapper-only tail (`python/arclink_notification_delivery.py:655`, `python/arclink_notification_delivery.py:661`). It does not validate `-u 0:0 ... arclink_public_agent_bridge.py`.

REFINE on secrets: wrapper sees the bot token in stdin and uses it for Telegram `getMe` (`python/arclink_public_agent_bridge_root.py:115`, `python/arclink_public_agent_bridge_root.py:90`), then passes the same raw payload to the unprivileged child (`python/arclink_public_agent_bridge_root.py:122`). I do not see new logging of the token; detached job files strip `bot_token` before disk (`python/arclink_notification_delivery.py:1162`) and hydrate it at runtime (`python/arclink_notification_delivery.py:1170`). The preload env contains bot profile JSON, not the token (`python/arclink_public_agent_bridge_root.py:105`).

### 3. D5 + durability
CONFIRM D5 evidence: the bridge only reports delivered when status is confirmed and it has message ids (`python/arclink_public_agent_bridge.py:102`, `python/arclink_public_agent_bridge.py:116`), and notification normalization also requires ids for delivered=true (`python/arclink_notification_delivery.py:437`, `python/arclink_notification_delivery.py:450`).

REFINE reaper: it should not double-deliver a healthy in-flight worker. The lease claim is atomic on due rows (`python/arclink_notification_delivery.py:2143`), the reaper only considers leased future rows (`python/arclink_notification_delivery.py:1395`, `python/arclink_notification_delivery.py:1401`), skips non-stale leases (`python/arclink_notification_delivery.py:1409`), and skips active worker pids (`python/arclink_notification_delivery.py:1421`). It can still duplicate if a worker dies after a real platform send but before marking `delivered_at`; no code here can prove idempotence after an external side effect.

CONFIRM/REFINE failed+no-id behavior: unknown is held, and failed+ids is held, but failed+no-ids is normal retry (`python/arclink_notification_delivery.py:464`, `python/arclink_notification_delivery.py:468`). That can duplicate a delivered-but-timed-out send with no captured message id. Jitter is real for normal retry: sha256 over notification id and attempt spreads around the base delay (`python/arclink_control.py:9854`, `python/arclink_control.py:9861`), then `mark_notification_error` schedules that value (`python/arclink_control.py:9885`).

### 4. L2 cache
CONFIRM mostly fail-closed: cache requires the flag (`python/arclink_public_agent_bridge.py:288`), HMACs the bot token (`python/arclink_public_agent_bridge.py:401`), returns no cache path if the secret is empty (`python/arclink_public_agent_bridge.py:487`), rejects agent-root paths (`python/arclink_public_agent_bridge.py:423`), requires uid 0 and mode 0700 (`python/arclink_public_agent_bridge.py:464`, `python/arclink_public_agent_bridge.py:466`), and falls back to live `bot.initialize()` when cache is unavailable (`python/arclink_public_agent_bridge.py:611`).

REFINE: the root wrapper can create/chmod the configured cache dir before owner validation (`python/arclink_public_agent_bridge.py:456`, `python/arclink_public_agent_bridge.py:460`). That is operator-env controlled, not chat controlled, but parent/dir ownership validation before mutation would be tighter.

### 5. Biggest residual risk + what was missed
Biggest missed production bug: Telegram native replay appears broken. Telegram ingress preserves `telegram_update_json` for normal messages (`python/arclink_telegram.py:1646`, `python/arclink_telegram.py:1650`), payload forwarding preserves it (`python/arclink_notification_delivery.py:897`), replay handlers return `True` after handling text/callback/media (`python/arclink_public_agent_bridge.py:1036`, `python/arclink_public_agent_bridge.py:1052`, `python/arclink_public_agent_bridge.py:1067`), but `_run_telegram` then unconditionally calls `adapter.handle_message(event)` even though `event` is only defined under `if not replayed` (`python/arclink_public_agent_bridge.py:921`, `python/arclink_public_agent_bridge.py:928`). That can turn successful native handling into an exception, then retry, with duplicate-send risk.

Also missed: the broker env omission means L2 may not actually engage in the main trusted-host Docker path, masking the flag-on wrapper-skew outage instead of solving it.
<<<CODEX-REVIEW-END>>>
