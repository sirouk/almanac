<<<CODEX-PHASE1-START W2>>>
## W2 (CANON-08 D3) — Codex implementation report
### Changed
- `python/arclink_hosted_api.py`: added explicit `ARCLINK_TRUSTED_PROXY_CIDRS` handling; fleet callback now receives `remote_addr` and resolves `source_ip` through `_remote_ip_from_headers`.
- `python/arclink_fleet_enrollment.py`: added optional `source_ip_trust` metadata.
- `tests/test_arclink_fleet_enrollment.py`: added and wired four callback source-IP/token regressions.
- `tests/test_arclink_hosted_api.py`: added shared resolver proof for unset trusted proxy CIDRs.
- `docs/API_REFERENCE.md`: documented trusted-proxy CIDR behavior and proxy sanitization responsibility.
### FINAL-PLAN mapping
- Callback dispatch passes `remote_addr`; callback resolves via `_remote_ip_from_headers`.
- Forwarded headers are trusted only from `ARCLINK_TRUSTED_PROXY_CIDRS`; unset ignores XFF/X-Real-IP.
- Callback stores `source_ip=resolved or "unverified"` and `source_ip_trust`.
- Bearer token/HMAC authorization remains unchanged.
### Old hole closed (proof)
- Spoofed XFF/X-Real-IP from an untrusted peer records the direct peer, not client headers.
- Trusted proxy CIDR records the first forwarded client IP.
- Missing `remote_addr` records `unverified`.
- Invalid Bearer token fails even with trusted-proxy forwarded headers; valid token succeeds from arbitrary direct IP.
### Tests added + results
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_fleet_enrollment.py`
  - `PASS all 18 ArcLink fleet enrollment tests`
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_hosted_api.py`
  - `PASS all ArcLink hosted API tests`
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_documentation_truths.py`
  - `PASS all documentation truth tests`
- Report written to `research/canon/phase1/W2.fix.md`.
### Residual / NEEDS-DECISION
- None for W2. Trusted reverse proxy header sanitization remains the planned CANON-02 / PG-FLEET / PG-INGRESS concern.
<<<CODEX-PHASE1-END W2>>>
