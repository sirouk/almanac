<<<CODEX-PHASE1-START W5>>>
## W5 (CANON-18 D1) — Codex implementation report
### Changed
- `python/arclink_notion_webhook.py:378` in `Handler.do_POST`: after stored-token HMAC verification, signed events now read `notion_webhook_verified_at`; if empty, the handler returns HTTP `412` with the required operator-confirmation message.
- `tests/test_arclink_notion_webhook.py:317`: added the unconfirmed signed-event regression.
- `tests/test_arclink_notion_webhook.py:363`: added the confirmed signed-event acceptance regression.
- `tests/test_arclink_notion_webhook.py:448`: wired both new tests into the direct `__main__` runner.
### FINAL-PLAN mapping
- Plan step 1: `python/arclink_notion_webhook.py:378-384` reads `NOTION_WEBHOOK_VERIFIED_AT_KEY` after `notion_verify_signature(...)` and before `store_notion_event(...)` at `:391`; empty confirmation fails closed with HTTP `412` and preserves state.
- Plan step 2: `tests/test_arclink_notion_webhook.py:317-356` proves rejection before `webhook-confirm-verified`; `tests/test_arclink_notion_webhook.py:363-401` proves acceptance after operator confirmation.
### Old hole closed (proof)
- A valid-HMAC signed event with a stored token but empty `notion_webhook_verified_at` now returns `412`, leaves `notion_webhook_events` at `0`, preserves the stored token, does not set verified state, and does not kick the SSOT batcher.
### Tests added + results
- Added `test_signed_event_requires_operator_confirmation_and_preserves_token`.
- Added `test_signed_event_is_accepted_after_operator_confirmation`.
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_notion_webhook.py` → `PASS all 9 ArcLink notion webhook regression tests`
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_loopback_service_hardening.py` → `PASS all 6 loopback hardening regression tests`
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_documentation_truths.py` → `PASS all documentation truth tests`
- `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 -m py_compile python/arclink_notion_webhook.py tests/test_arclink_notion_webhook.py` → pass
### Residual / NEEDS-DECISION
- No W5 needs-decision. Leg A nonce-bearing install URL remains the separate CANON-18 D1 follow-on.
- I also tried `tests/test_arclink_ctl_notion.py`; it fails before the webhook-confirm tests on an unrelated readonly default DB path in `test_notion_preflight_root_cli_uses_root_page_id`, not from this W5 change.
<<<CODEX-PHASE1-END W5>>>
