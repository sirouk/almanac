<<<CODEX-PHASE1-START W6>>>
## W6 (CANON-02,CANON-03 D2,D1) — Codex implementation report
### Changed
- `python/arclink_hosted_api.py:215-217` now loads `ARCLINK_TRUSTED_PROXY_CIDRS`, `ARCLINK_ADMIN_ALLOWED_CIDRS`, and `ARCLINK_BACKEND_ALLOWED_CIDRS` as separate config fields.
- `python/arclink_hosted_api.py:682-693` keeps `_backend_client_allowed()` on backend-control CIDRs and adds `_admin_client_allowed()` for Operator admin CIDRs with loopback-only default.
- `python/arclink_hosted_api.py:4152-4162` gates CIDR-protected admin routes on `_admin_client_allowed()`; `python/arclink_hosted_api.py:4203-4210` gates the `/auth/login` admin branch on the same admin policy.
- Canonical rendering now writes the split: `compose.yaml:53-55`, `bin/docker-entrypoint.sh:391-393` and `:462-464`, and `bin/deploy.sh:8488-8490`.
- Docs now name the two roles and the backend-only residual role: `docs/API_REFERENCE.md:90-100` and `:226-228`, `docs/arclink/sovereign-control-node.md:299-304`, `docs/arclink/control-node-production-runbook.md:44` and `:62-63`.

### FINAL-PLAN mapping
- CANON-03 D1 config split: `HostedApiConfig.admin_allowed_cidrs` is distinct from `trusted_proxy_cidrs`; admin route and unified-login admin decisions use `_admin_client_allowed()`, not proxy trust or backend peer trust.
- CANON-02 D2 F1=A self-heal: blessed Compose, Docker entrypoint, and deploy runtime config render `ARCLINK_TRUSTED_PROXY_CIDRS=172.16.0.0/12` by default while keeping `ARCLINK_ADMIN_ALLOWED_CIDRS` unset/loopback-only by default. No migration shim was added.
- W2 resolver boundary preserved: `_remote_ip_from_headers()` still trusts forwarded headers only through `ARCLINK_TRUSTED_PROXY_CIDRS`; W6 did not reintroduce backend/admin CIDRs into the resolver.

### Old hole closed (proof)
- `tests/test_arclink_hosted_api.py:5329-5341` proves a Docker bridge proxy can be trusted for XFF and backend-peer checks but is not admin-allowed; a trusted proxy without XFF now gets `403` on `/admin/dashboard`.
- `tests/test_arclink_hosted_api.py:5343-5357` proves an admin-allowed direct peer does not become a trusted proxy; spoofed XFF is ignored and the direct admin IP is used.
- `tests/test_arclink_hosted_api.py:5359-5379` proves unset `ARCLINK_TRUSTED_PROXY_CIDRS` still fails closed for proxied admin XFF: resolver returns the direct bridge peer and admin returns `403`.
- `tests/test_arclink_hosted_api.py:5391-5422` proves `/auth/login` cannot mint an admin session from proxy/backend CIDRs without an allowed resolved client IP, and cannot mint from XFF when trusted proxy CIDRs are unset.

### Tests added + results
- Added/expanded hosted API regression matrix in `tests/test_arclink_hosted_api.py:5264-5422`.
- Added canonical rendering regression in `tests/test_deploy_regressions.py:1005-1036`, wired into the direct runner at `tests/test_deploy_regressions.py:4765-4766`.
- Results:
  - `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_hosted_api.py` -> `PASS all ArcLink hosted API tests`
  - `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_deploy_regressions.py` -> `PASS all 139 deploy regression tests`
  - `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_docker.py` -> `PASS all ArcLink Docker regression tests`
  - `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_documentation_truths.py` -> `PASS all documentation truth tests`
  - `TERMINAL_DISABLE_TMUX=1 ARCLINK_FLEET_WORKER_CONFIG=/tmp/none python3 tests/test_arclink_fleet_enrollment.py` -> `PASS all 18 ArcLink fleet enrollment tests`
  - `python3 -m py_compile python/arclink_hosted_api.py tests/test_arclink_hosted_api.py tests/test_deploy_regressions.py && bash -n bin/docker-entrypoint.sh bin/deploy.sh deploy.sh` -> pass

### Residual / NEEDS-DECISION
- No W6 blocker. Traefik `forwardedHeaders.trustedIPs`, Next rewrite dev-gating, and a live `PG-ADMIN-CIDR-XFF` proof remain outside this scoped implementation item unless assigned in a follow-up.
<<<CODEX-PHASE1-END W6>>>
