# CANON-05 — Public Bots (Telegram/Discord)

## PIECE
This piece is the ArcLink **public Captain bot subsystem**: the Telegram and Discord transports, the shared Raven turn engine that parses every public-channel message into one of ~33 commands or routes it to the live Hermes Agent, and the deploy-time command registration. It owns exactly four tracked files:
- `python/arclink_public_bots.py` (7917 lines) — the channel-neutral Raven turn engine `handle_arclink_public_bot_turn` (`python/arclink_public_bots.py:7125`), the registered action table `ARCLINK_PUBLIC_BOT_ACTIONS` (`:342`), pricing/plan constants, onboarding/checkout copy, channel pairing, selected-agent bridge enqueue (`_aboard_freeform_reply` `:3971` → `_queue_public_agent_turn` `:3905`), Crew Training, Academy Mode, share approve/deny/accept/claim/create, credentials handoff, retire-agent, refuel, agent identity/rename, Raven display name, and the Telegram/Discord reply-markup/component renderers.
- `python/arclink_telegram.py` (1775 lines) — Telegram Bot API client, webhook entry `handle_telegram_update` (`:1447`), per-chat command-scope planning (`arclink_public_bot_telegram_active_command_plan` `:554`), Operator Raven interception (`_handle_operator_telegram_update` `:1287`), the live/fake transports, and the long-poll loop `run_telegram_polling` (`:1701`).
- `python/arclink_discord.py` (610 lines) — Discord interaction handler `handle_discord_interaction` (`:414`), Ed25519 signature verify (`:230`), timestamp tolerance (`:250`), interaction dedupe into `arclink_webhook_events` (`:261`), slash/component parsing (`:314`), webhook entry `handle_discord_webhook_request` (`:530`), live/fake transports.
- `python/arclink_public_bot_commands.py` (451 lines) — deploy-time `register_public_bot_commands` (`:359`) and active per-chat scope refresh (`refresh_active_telegram_command_scopes` `:217`) that `docker exec`s into running gateway containers.

All four listed files exist and are tracked. No additional clearly-belonging file was found outside the listed set (the hosted-API webhook handlers and the delivery worker live in adjacent pieces CANON-02 and CANON-23).

## INPUT CONTRACT (code-verified)
**Telegram webhook update** — `handle_telegram_update(conn, update: Mapping, *, stripe_client, price_id, founders_price_id, scale_price_id, additional_agent_price_id, sovereign_agent_expansion_price_id, scale_agent_expansion_price_id, base_domain, telegram_bot_token, env)` (`arclink_telegram.py:1447`). `update` is the raw Telegram Bot API JSON object. `parse_telegram_update` (`:1061`) extracts `chat_id`/`user_id`/`text` from `callback_query` (data with `arclink:` prefix stripped; `telegram_native_callback` flag set when NOT prefixed, `:1071`) or `message`/`edited_message`. Returns `None` (ignored, HTTP 200) when no `chat_id` or no `text` (`:1102`). 11 non-text message kinds get `[Telegram …]` fallback text (`_telegram_message_kind` `:1018`, `_telegram_fallback_text_for_kind` `:1044`). Caller is the hosted-API route `POST /api/v1/webhooks/telegram` → `_handle_telegram_webhook` (`arclink_hosted_api.py:2879`), which first **rejects** any request lacking a matching `X-Telegram-Bot-Api-Secret-Token` header (401, `:2900`) and returns 503 if `TELEGRAM_WEBHOOK_SECRET` is unset (`:2889`).

**Discord webhook request** — `handle_discord_webhook_request(conn, *, body: str, signature, timestamp, config: DiscordConfig, stripe_client, price_id, …)` (`arclink_discord.py:530`). Validates timestamp tolerance FIRST (`_validate_discord_timestamp` `:250`, raises on non-int or >tolerance), then Ed25519 signature (`verify_discord_signature` `:230`, raises `ArcLinkDiscordError` on failure), then reserves the interaction id for dedupe (`:556`), then dispatches. `parse_discord_interaction` (`:314`) maps type 1 PING→None, type 2 slash (named-option grammar for agent/raven/email/name/plan/refuel/top-up/pair-channel/link-channel/raven-name + a generic ordered-option reconstruction `:366`), type 3 component (custom_id with `arclink:` stripped `:386`), and gateway `content` fallback (`:400`). Caller is `POST /api/v1/webhooks/discord` → `_handle_discord_webhook` (`arclink_hosted_api.py:3016`), which returns 503 if `public_key` unset and passes `X-Signature-Ed25519`/`X-Signature-Timestamp` headers.

**Channel-neutral turn** — `handle_arclink_public_bot_turn(conn, *, channel, channel_identity, text, stripe_client=None, price_id="price_arclink_sovereign", founders_price_id="price_arclink_founders", scale_price_id="", additional_agent_price_id="", sovereign_agent_expansion_price_id="", scale_agent_expansion_price_id="", base_domain="", metadata=None, display_name_hint="")` (`arclink_public_bots.py:7125`). `channel` must be in `{telegram, discord}` (`_clean_channel` `:750` raises `ArcLinkPublicBotError` otherwise — note **web is NOT accepted** here, `:78`). `channel_identity` must be non-empty (`:757`). First action is the rate-limit check (`:7144`). Validation of plan strings via `ARCLINK_PUBLIC_BOT_PLANS` (`:7839`). `display_name_hint` is truncated to 40 chars (`:7147`).

**Deploy-time registration** — `register_public_bot_commands(env=None)` (`arclink_public_bot_commands.py:359`). Reads merged env (incl. `ARCLINK_CONFIG_FILE` shell file, `:53`). Telegram branch runs only if `TelegramConfig.bot_token` truthy (`:364`); Discord branch only if both `bot_token` and `app_id` truthy (`:387`). Each platform wrapped in its own try/except so one platform failing does not block the other (`:382`, `:390`). `main()` (`:397`) is the CLI used by `deploy.sh control install|upgrade`; exit code 1 if any platform errored (`:447`).

## OUTPUT CONTRACT (code-verified)
**`ArcLinkPublicBotTurn`** dataclass (`arclink_public_bots.py:730`): `channel, channel_identity, session_id, status, current_step, action, reply, checkout_url="", user_id="", deployment_id="", bot_display_name="Raven", buttons=(), telegram_reply="", telegram_entities=()`. `to_dict()` via `asdict` (`:746`).

**Telegram webhook result dict** (`arclink_telegram.py:1525`): `{chat_id, text, entities, reply_markup, session_id, action, channel_identity, telegram_message_id, callback_query_id, callback_message_id, command_scope}`. `reply_markup` from `arclink_public_bot_turn_telegram_reply_markup` (`arclink_public_bots.py:1738`) which only emits markup when status is ready and rewrites button commands via `_active_raven_callback_command` (`:1677`). When no explicit entities, backtick code-spans are converted to Telegram entities (`telegram_markdown_to_entities` `:1237`, called at `arclink_telegram.py:1523`).

**Discord interaction response** (`arclink_discord.py:483`): `{type: 4 (CHANNEL_MESSAGE_WITH_SOURCE), data: {content, components?}, session_id, action, channel_identity}`. Type 7 (UPDATE_MESSAGE) is returned instead when `itype==3 and action=="credentials_stored"` (`:475`) to scrub the credential message in-place. Empty content on a queued agent turn is replaced with `"Sent to your active Hermes Agent."` (`:468`) because Discord interaction callbacks cannot have empty content.

**Side effects / DB writes:**
- `notification_outbox` INSERT with `target_kind="public-agent-turn"` via `_queue_public_agent_turn` → `queue_notification` (`arclink_public_bots.py:3952`, producer `arclink_control.py:8055`). This is the asynchronous selected-agent bridge.
- `arclink_events` INSERT `public_bot:agent_turn_queued` (`:3962`), `public_bot:agent_bridge_intro_sent` (`_claim_agent_bridge_intro` `:1174`), `public_bot:pair_channel_claimed`, `public_bot:share_grant_*`.
- `arclink_onboarding_sessions` INSERT/UPDATE via the CANON-04 helpers (`create_or_resume`, `answer_arclink_onboarding_question`, etc.).
- `arclink_channel_pairing_codes` INSERT/UPDATE (`_create_pair_channel_code` `:4084`, schema `arclink_control.py:1355`).
- `arclink_public_bot_identity` UPSERT (Raven display name, `_store_raven_display_name` `:1093`, schema `:1344`).
- `arclink_webhook_events` INSERT for Discord dedupe (`arclink_discord.py:272`, schema `arclink_control.py:980`).
- `arclink_credential_handoffs` UPDATE to `removed` on credentials-stored ack (`_credentials_stored_reply` `:3754`).
- `notification_outbox` UPDATE (delivered_at + delivery_error) to cancel pending agent turns on retire (`_cancel_pending_public_agent_turns_for_deployment` `:2803`).
- `rate_limits` INSERT/COUNT for every non-operator turn (`check_arclink_rate_limit` `arclink_api_auth.py:425`).

**Wire side effects (live mode only):** Telegram `setMyCommands`/`setWebhook` (`arclink_telegram.py:351`/`:794`); Discord `PUT /applications/.../commands` (`arclink_discord.py:173`); outbound replies through `LiveTelegramTransport`/`LiveDiscordTransport`.

## TOUCH POINTS
**Env vars (read):**
- Telegram: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, `TELEGRAM_WEBHOOK_URL`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_API_BASE` (`TelegramConfig.from_env` `arclink_telegram.py:944`).
- Discord: `DISCORD_BOT_TOKEN`, `DISCORD_APP_ID`, `DISCORD_PUBLIC_KEY` (must be 64-hex, `DISCORD_PUBLIC_KEY_RE` `:38`), `DISCORD_TEST_GUILD_ID`, `DISCORD_WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS` (clamped 1..3600, `:209`) (`DiscordConfig.from_env` `arclink_discord.py:191`).
- Operator gate: `OPERATOR_NOTIFY_CHANNEL_PLATFORM`, `OPERATOR_NOTIFY_CHANNEL_ID`, `ARCLINK_OPERATOR_TELEGRAM_USER_IDS`, `ARCLINK_CURATOR_CHANNELS` (`arclink_telegram.py:1123`–`:1141`).
- Hermes source discovery: `ARCLINK_HERMES_AGENT_SRC`, `ARCLINK_HERMES_SOURCE_DIR`, `HERMES_AGENT_SRC`, `ARCLINK_RUNTIME_DIR`, `RUNTIME_DIR`, `STATE_DIR`, `ARCLINK_STATE_DIR`, `ARCLINK_PRIV_DIR`, `CONFIG_PRIV_DIR` (`_hermes_agent_source_candidates` `:420`).
- Commands deploy: `ARCLINK_CONFIG_FILE`, `ARCLINK_DB_PATH`, `STATE_DIR`, `ARCLINK_DOCKER_CONTAINER_PRIV_DIR`, `ARCLINK_DOCKER_HOST_PRIV_DIR`, `ARCLINK_CONTROL_COMPOSE_PROJECT` (`arclink_public_bot_commands.py:53`–`:116`).
- `ARCLINK_RELEASE_STATE_FILE` (operator upgrade-check local runner `arclink_telegram.py:1257`).

**DB tables r/w:** `arclink_onboarding_sessions` (schema `arclink_control.py:1309`), `arclink_onboarding_events`, `arclink_deployments` (`:991`), `notification_outbox` (`:745`), `arclink_webhook_events` (`:980`), `arclink_public_bot_identity` (`:1344`), `arclink_channel_pairing_codes` (`:1355`), `arclink_share_grants` (`:1052`), `arclink_credential_handoffs` (`:1034`), `rate_limits`, `refresh_jobs`.

**Sockets/ports:** Telegram `https://api.telegram.org` (HTTP via `arclink_http.http_request`, `allow_loopback_http=False` `arclink_telegram.py:197`). Discord `https://discord.com/api/v10` (`arclink_discord.py:35`).

**Subprocess argv:** `docker ps --filter label=com.docker.compose.project=… --filter label=…service=… --format {{.Names}}` and `docker exec <container> bash -lc 'PYTHONPATH=… python3 - <<PY … telegram_menu_commands …'` (`arclink_public_bot_commands.py:129`, `:155`). These run during deploy-time command refresh only.

**Locks/concurrency:** in-process `_TELEGRAM_CHAT_COMMAND_SCOPE_CACHE` set (`arclink_telegram.py:50`); the public-agent-turn outbox row is claimed with a DB lease in the delivery worker (CANON-23). Discord interaction dedupe relies on the `(provider, event_id)` PK + `sqlite3.IntegrityError` (`arclink_discord.py:281`).

**External services:** Telegram Bot API, Discord API, Stripe (injected `stripe_client`, CANON-07), the running Hermes gateway container (CANON-12/CANON-19/CANON-30).

**Secrets handling:** `nacl.signing.VerifyKey` for Discord (`arclink_discord.py:242`). Test-sentinel public keys are rejected so an unset config cannot become an unauthenticated webhook (`:239`). Telegram webhook secret compared with `hmac.compare_digest` (`arclink_hosted_api.py:2900`). Credentials revealed once then handoff marked removed (`_credentials_stored_reply`). `_reject_secret_material` in the onboarding producer blocks storing secret-looking values (CANON-04).

## CODE-PATH TRACE (Telegram first-contact → checkout, then aboard → agent bridge)
1. `POST /api/v1/webhooks/telegram` hits `_handle_telegram_webhook` (`arclink_hosted_api.py:2879`); rejects unless `X-Telegram-Bot-Api-Secret-Token` matches `config.telegram_webhook_secret` via `hmac.compare_digest` (`:2900`).
2. `handle_telegram_update(conn, body, …, telegram_bot_token=…, env=…)` (`arclink_telegram.py:2903` call site; def `:1447`).
3. `parse_telegram_update(update)` (`:1467`) → `{chat_id,user_id,chat_type,text,telegram_update_kind,telegram_update_json,…}` or `None`.
4. `_handle_operator_telegram_update(conn, parsed, env)` (`:1471`). If `operator_telegram_sender_allowed` (`:1144`) is false it returns `None` and falls through. **Operator path returns BEFORE the public-bot turn, so it bypasses `_check_public_bot_rate_limit`.**
5. `handle_arclink_public_bot_turn(conn, channel="telegram", channel_identity="tg:<user_id>", text=…, stripe_client=…, …)` (`:1485`).
6. `_check_public_bot_rate_limit` → `check_arclink_rate_limit(scope="onboarding:telegram", subject=identity, limit=20, window=900)` (`arclink_public_bots.py:7144`/`:1972`). On overflow raises `ArcLinkRateLimitError` which propagates to the dispatcher and returns HTTP 429 (`arclink_hosted_api.py:4183`) — **fail-closed**, Telegram retries.
7. `_raven_control_rewrite(message, command)` (`:7156`) rewrites `/raven <verb>`, `/arclink`, `/arclink_control`, `/arclink_ops\d{0,2}` into the underlying `/command` and sets `raven_control_requested=True`.
8. Bare-slash-to-agent gate (`:7162`): if message starts with `/`, not raven-control, there is a READY deployment, and the command name ∈ active agent inventory, route to `_aboard_freeform_reply(source_kind="agent_command")` (`:7189`); `update` → `_upgrade_hermes_reply` (`:7174`).
9. First contact (no deployment): falls to `create_or_resume_arclink_onboarding_session` (`:7716`) then greeting `"{raven} on the line, Captain."` / `"Captain {name}, {raven} on the line."` (`:7728`) via `_identity_or_package_prompt`.
10. `/plan founders|sovereign|scale` (`:7837`) → `answer_arclink_onboarding_question(question_key="plan")` then, if `stripe_client` present, `_open_first_agent_checkout_turn` (`:7868`); else a `prompt_checkout` reply. Direct-checkout buttons (`_ensure_direct_checkout_buttons` `:1437`) mint SHA-256 verifier digests into session metadata key `public_bot_checkout_verifiers` (`:1455`) and build URLs to `ARCLINK_PUBLIC_BOT_DIRECT_CHECKOUT_PATH`.
11. After provisioning (deployment status ∈ `{active, first_contacted}`), the aboard routing law (`:7676`) routes a non-Raven message to `_aboard_freeform_reply` (`:7691`), enqueuing a `notification_outbox` row `target_kind="public-agent-turn"`, `channel_kind="telegram"`, `target_id=channel_identity`, with `extra={deployment_id, prefix, user_id, agent_label, raven_display_name, helm_url, source_kind, telegram_reply_to_message_id?, telegram_update_kind?, telegram_update_json?, telegram_native_callback?}` (`:3919`–`:3959`). Webhook returns near-empty text + `action="agent_message_queued"`.
12. Back in `handle_telegram_update`, if `telegram_bot_token` present, per-chat command scope is refreshed (`refresh_arclink_public_telegram_chat_commands` `:1509`), forced when `action=="switch_agent"`.
13. `run_telegram_polling` (fake/CLI path) additionally sends a `👀` reaction + typing action on `agent_message_queued` before the (empty) reply (`:1755`).
14. The `public-agent-turn` row is consumed by the CANON-23 delivery worker `_deliver_public_agent_turn` (`arclink_notification_delivery.py:1827`), which reads `extra` keys `telegram_reply_to_message_id` (`:650`), `discord_channel_id` (`:655`), `agent_label`/`display_name` (`:682`), and `telegram_update_json` (`:689`) and drives the public agent bridge broker (CANON-12).

## CROSS-PIECE CONTRACTS (both ends verified)
1. **→ CANON-12/CANON-23 selected-agent bridge.** Producer: `_queue_public_agent_turn` → `queue_notification` writes `notification_outbox(target_kind="public-agent-turn", target_id=channel_identity, channel_kind=channel, message, extra_json={deployment_id, agent_label, raven_display_name, helm_url, source_kind, telegram_reply_to_message_id, telegram_update_kind, telegram_update_json, telegram_native_callback, discord_channel_id, discord_user_id, discord_message_id, discord_chat_type})` (`arclink_public_bots.py:3919`–`3959`, `arclink_control.py:8055`). Consumer: `_deliver_public_agent_turn` reads exactly these keys (`arclink_notification_delivery.py:650/655/682/689`) and `_run_public_agent_turn`/bridge (`arclink_hosted_api.py:568`). **BOTH-ENDS-VERIFIED: yes** for the outbox row shape and the telegram_reply_to_message_id/discord_channel_id/telegram_update_json keys; the inner prompt grammar handed to the gateway is CANON-12's.
2. **→ CANON-04 onboarding.** Producer: this piece imports and calls `create_or_resume_arclink_onboarding_session`, `answer_arclink_onboarding_question`, `cancel_arclink_onboarding_session`, `handoff_arclink_onboarding_channel`, `open_arclink_onboarding_checkout`, `default_arclink_agent_profile`, `clean_arclink_agent_name/title` (`arclink_public_bots.py:61`). Consumer/owner: `arclink_onboarding.py` (def at `:421`, `:513`, `:841`). The session dict keys consumed here (`session_id`, `status`, `current_step`, `metadata_json`, `display_name_hint`, `selected_plan_id`) match the INSERT columns (`arclink_onboarding.py:479`). **BOTH-ENDS-VERIFIED: yes.**
3. **→ CANON-02 hosted API (Telegram webhook secret).** Producer: `ensure_arclink_public_telegram_webhook` sets the Telegram webhook with `secret_token` (`arclink_telegram.py:832`, requires `TELEGRAM_WEBHOOK_SECRET` `:831`). Consumer: `_handle_telegram_webhook` compares `X-Telegram-Bot-Api-Secret-Token` to `config.telegram_webhook_secret` (`arclink_hosted_api.py:2900`). Both read the same env var `TELEGRAM_WEBHOOK_SECRET`. **BOTH-ENDS-VERIFIED: yes.**
4. **→ CANON-02 hosted API (direct-checkout token).** Producer: `_ensure_direct_checkout_buttons` stores `public_bot_checkout_verifiers={plan: sha256(token)}` in session metadata and builds the URL with `token=<raw>` (`arclink_public_bots.py:1437`–`:1486`). Consumer: `_public_bot_checkout_token_valid` re-hashes the supplied token and `hmac.compare_digest`s against the stored digest (`arclink_hosted_api.py:799`). Path constant `ARCLINK_PUBLIC_BOT_DIRECT_CHECKOUT_PATH` matches the route `("GET","/onboarding/public-bot-checkout")` (`arclink_hosted_api.py:3758`). **BOTH-ENDS-VERIFIED: yes.**
5. **→ CANON-14 Operator Raven.** Producer: `_handle_operator_telegram_update` dispatches via `dispatch_operator_raven_command(conn, dispatch_text, env, upgrade_check_runner, actor_id, idempotency_key)` and gates mutating commands with `operator_raven_command_is_mutating` + `strip_operator_approval_code` (`arclink_telegram.py:1305`). Consumer/owner: `arclink_operator_raven.py`. Free-form falls to `enqueue_operator_agent_turn` (CANON-14, `arclink_operator_agent.py:273` also writes a `public-agent-turn` row). **BOTH-ENDS-VERIFIED: partial** — the dispatch call shape is verified here; the operator command catalog and the `--confirm` semantics live in CANON-14 and were not re-read line-by-line.
6. **→ CANON-30 Hermes command inventory.** Producer/owner: Hermes `hermes_cli.commands.telegram_menu_commands`. Consumer: `_load_hermes_telegram_menu_commands` (`arclink_telegram.py:463`) and the docker-exec script (`arclink_public_bot_commands.py:149`). Falls back to the 33-entry static list (`ARCLINK_TELEGRAM_AGENT_FALLBACK_COMMANDS` `:52`) when the source dir / container is absent. **BOTH-ENDS-VERIFIED: no** — the consumer is defensive (try/except → fallback); the live Hermes helper signature `telegram_menu_commands(max_commands)` returning `(list[(name,desc)], hidden)` is assumed, not read (proof-gated PG-HERMES).

## CODE vs COMMENT/DOC/NAME DRIFT
1. **Operator Telegram command catalog drift (prior doc 04 + raven-public-bot.md are STALE).** Prior doc lists 12 operator commands; code `ARCLINK_OPERATOR_TELEGRAM_COMMANDS` has **20** (`arclink_telegram.py:149`): operator_status, agents, operator_fleet, worker_probe, user_lookup, billing_status, backup_status, workspace_status, pod_repair, upgrade_check, upgrade_policy, upgrade, pin_upgrade, upgrade_sweep, fleet_drain, fleet_resume, rollout, action_status, academy_status, academy_roster. Prior doc claimed `fleet_list` is registered and `operator_fleet` is "not in the list" — the CODE registers `operator_fleet`, not `fleet_list`. CODE WINS.
2. **Registered public action count drift.** Prior doc 04 §C lists ~27 commands and omits `share_create`, `share_approve`, `share_deny`, `share_accept`, `add_agent`, `retire_agent`. The code `ARCLINK_PUBLIC_BOT_ACTIONS` defines **33** actions (`arclink_public_bots.py:342`–`:713`) including all six. CODE WINS.
3. **Legacy-Raven strip list drift.** Prior doc 04 §A2 lists 18 `ARCLINK_TELEGRAM_LEGACY_RAVEN_COMMAND_NAMES`; the code set has **~55** entries (`arclink_telegram.py:91`–`:148`, e.g. adds academy, add_agent, share_*, retire_agent, quick_training, keep_agent, delete_agent, remove_agent, tour, standard_packages, top_up, packages, setup_backup). CODE WINS.
4. **Bridge file misattribution.** Prior memory/decomposition implies `public-agent-turn` is consumed by `arclink_public_agent_bridge.py`. Grep proves NO `public-agent-turn` string in that file; the outbox row is read by `arclink_notification_delivery.py` (`:1827`) which then invokes the bridge broker in `arclink_hosted_api.py`. The bridge module is the inner executor, not the queue consumer.
5. **"Operator commands list" intro-copy vs registry.** `_operator_raven_intro_reply` advertises `/operator_fleet`, `/billing_status`, `/backup_status`, `/workspace_status`, `/upgrade_policy`, `/upgrade_sweep`, `/fleet_drain`, `/fleet_resume` (`arclink_telegram.py:1220`) — these now DO match the expanded registry, so the prior doc's "intro copy lists commands not in the set" note is itself stale.
6. **Docstring "1559/588/406/903 lines" in prior doc** vs actual 1775/610/451 — files grew; counts are stale (informational).
7. **`telegram_send_message` truncation docstring absent but real:** text >4000 chars is silently truncated to 3997+"…" (`arclink_telegram.py:224`) — no comment warns of this lossy path.

## ADVERSARIAL SELF-CHECK
1. **"Operator path bypasses the per-turn rate limit."** I traced that `_handle_operator_telegram_update` returns before `handle_arclink_public_bot_turn` (`arclink_telegram.py:1471`→`1485`) and found no operator-specific call to `check_arclink_rate_limit`. Falsifier: a rate-limit call inside `dispatch_operator_raven_command` or a webhook-layer limit that already covers operators. The dispatch-layer `_check_webhook_rate_limit` (`arclink_hosted_api.py:3994`) DOES run for every webhook hit, so operators are IP/secret-rate-limited but not per-identity onboarding-limited. I did not read `_check_webhook_rate_limit`'s body.
2. **"Both ends of the public-agent-turn extra_json match."** I verified producer keys and four consumer reads (`telegram_reply_to_message_id`, `discord_channel_id`, `agent_label`, `telegram_update_json`). Falsifier: the consumer may require additional keys (e.g. `prefix`) that the producer omits in some branches, or `telegram_update_json_list` (`arclink_hosted_api.py:690`) which the producer never writes — I did not confirm the producer emits `telegram_update_json_list`.
3. **"Fake-adapter default holds with no token."** `TelegramConfig.is_live` = bool(token) (`:962`); `run_telegram_polling` RAISES without token unless a `FakeTelegramTransport` is injected (`:1721`). So "fake mode by default" is true for tests but the long-poll runner is NOT silently-fake — it hard-fails closed. Falsifier: a default `FakeTelegramTransport` construction somewhere I missed.
4. **"Discord dedupe is race-safe."** Relies on `(provider,event_id)` PK + `IntegrityError`. Falsifier: if `conn` isolation lets two concurrent INSERTs both succeed before commit; SQLite default serialization likely prevents this but I did not test concurrent writers.
5. **"Bare-slash routing only fires for ready deployments."** Verified the `status in READY_STATUSES` guard at `:7170`. Falsifier: `_agent_command_names_from_context` could return a stale/over-broad inventory letting a Raven-owned name leak to the agent; I read its callers but not its full body.

## OPEN FOR CODEX FEDERATION
1. Confirm `_deliver_public_agent_turn` (CANON-23) does NOT require any `extra_json` key that the producer `_queue_public_agent_turn` fails to emit (esp. `prefix`, `telegram_update_json_list`) — cross-check `arclink_notification_delivery.py:1827`+ against `arclink_public_bots.py:3919`.
2. Independently verify whether the Operator Telegram path is rate-limited at all (the per-identity `_check_public_bot_rate_limit` is bypassed; only the webhook-layer `_check_webhook_rate_limit` applies). Is that intended, given operators can issue mutating actions?
3. Verify the live Hermes `telegram_menu_commands(max_commands)` signature and `(cmds, hidden)` return shape against the pinned hermes-agent source (PG-HERMES) — both `arclink_telegram.py:463` and the docker-exec script `arclink_public_bot_commands.py:149` depend on it.
4. Confirm `verify_discord_signature` rejection of test-sentinel keys is actually exercised — is there a path where `DISCORD_PUBLIC_KEY` could be a valid-looking 64-hex test key that passes `is_live` but is not Discord's real key (TOCTOU between registration and webhook)?
5. The CANON-14 operator-raven mutating/`--confirm` gate and approval-code semantics: re-read `operator_raven_command_is_mutating` and `strip_operator_approval_code` to confirm a missing approval code truly blocks mutation (fail-closed).

## RISKS (severity-ranked, code-cited)
- **MEDIUM — Operator path skips per-identity rate limit.** `_handle_operator_telegram_update` returns before the `_check_public_bot_rate_limit` call (`arclink_telegram.py:1471` vs `arclink_public_bots.py:7144`), so a compromised/allowlisted operator id is only bounded by the coarse webhook-layer limit. Mutating actions (`/upgrade`, `/pod_repair`, `/rollout`) flow through here.
- **MEDIUM — Telegram webhook secret is the ONLY authn on the Telegram path.** A leaked `TELEGRAM_WEBHOOK_SECRET` lets any caller forge updates; there is no per-update signature like Discord's Ed25519 (`arclink_hosted_api.py:2900`). Fail-closed if unset (503, `:2889`) which is correct, but the single shared secret is the whole trust boundary.
- **LOW — Lossy silent truncation of long replies/updates.** `telegram_send_message` truncates >4000 chars (`arclink_telegram.py:224`) and `_telegram_update_json` drops `photo/entities/reply_to_message/quote` then truncates text to fit 60000 bytes before giving up to placeholder (`:1004`) — a very large rich message degrades silently.
- **LOW — Command-scope refresh depends on `docker ps`/`docker exec` and falls back silently.** `_agent_commands_from_gateway_container` swallows all exceptions → fallback list (`arclink_public_bot_commands.py:172`), so a broken container yields a stale 33-command menu with no operator-visible error beyond the drift notification (which only fires on conflicts, `:330`).
- **LOW — `_TELEGRAM_CHAT_COMMAND_SCOPE_CACHE` is process-local and unbounded.** (`arclink_telegram.py:50`) A long-lived process accumulates `(chat_id, signature)` tuples with no eviction; minor memory growth, and cache is not shared across workers (each worker re-registers once).
- **INFO — Discord global-vs-guild command registration is config-driven** (`arclink_discord.py:167`); a missing `DISCORD_TEST_GUILD_ID` registers globally (propagation delay) — expected behavior, not a defect.

## VERDICT
This piece provably does its job: the four files implement a complete, channel-neutral public-bot turn engine with a single authoritative routing law (`handle_arclink_public_bot_turn` `:7125`), two real transports with genuine authn (Telegram shared-secret header + Discord Ed25519 with sentinel rejection), idempotent Discord dedupe via a PK/IntegrityError, a clean fake-default for tests, and a well-defined asynchronous selected-agent bridge whose outbox-row contract I verified at both ends against the CANON-23 consumer. All four owned test suites pass (72 tests). Load-bearing strengths: fail-closed rate limiting (HTTP 429), fail-closed webhook authn (401/503), proof-gated live delivery (no network without tokens), and a conflict-free active-chat command-scope planner that yields one `/raven` gateway plus the live Hermes inventory. Real weaknesses: the Operator interception bypasses per-identity rate limiting; the Telegram path leans entirely on one shared secret with no per-message signature; live Hermes command discovery and the inner bridge prompt grammar are proof-gated (PG-HERMES / CANON-12) and could not be verified end-to-end here; and the prior ground-truth doc 04 is materially stale on the operator (20 vs 12) and public (33 vs 27) command catalogs and on which module consumes the `public-agent-turn` outbox row.
